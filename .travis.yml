language: node_js
node_js:
  - 12

branches:
  only:
    - master

notifications:
  email: false

before_script:
  - npm install

script:
  - npm run pack-install

before_deploy:
  - mv release/the-happy-app.dmg ./the-happy-app.dmg
  - git config --local user.name "$USER_NAME"
  - git config --local user.email "$USER_EMAIL"
  - git tag "$TRAVIS_BRANCH-$(git log --format=%h -1)"

deploy:
  provider: releases
  token: $GHTOKEN
  file:
    - "the-happy-app.dmg"
  release_notes: "$TRAVIS_BRANCH: $TRAVIS_COMMIT_MESSAGE"
  target_commitish: $TRAVIS_COMMIT
  skip_cleanup: true
  on:
    all_branches: true
